// Build entry point for the card chunk: everything the loader defers until a
// root element exists. The filename matches the emitted chunk, which is what
// the loader's relative import names.
import "./adjustable-bed-card";
